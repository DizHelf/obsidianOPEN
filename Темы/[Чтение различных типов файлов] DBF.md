
![[Pasted image 20241010122446.png]]

## Реализация

```js
&НаКлиенте
Процедура ПрочитатьФайл_DBF()

	ФайлДБФ = Новый XBase;
	ФайлДБФ.ОткрытьФайл(Объект.ПутьКФайлу);
	
	ДанныеФайла = Новый ДвоичныеДанные(Объект.ПутьКФайлу);
	АдресДанных = ПоместитьВоВременноеХранилище(ДанныеФайла);
	Данные = ПолучитьИзВременногоХранилища(АдресДанных);
	ПутьКФайлуНаСервере = ПолучитьИмяВременногоФайла("dbf");
	Данные.Записать(ПутьКФайлуНаСервере);
	
	Если ФайлДБФ.Первая() = Ложь Тогда
		Возврат;
	КонецЕсли;
	
	Пока ФайлДБФ.ВКонце() = Ложь Цикл
		НоваяСтрока = Объект.ТаблицаДанных.Добавить();
		НоваяСтрока.Наименование = ФайлДБФ.NAME;
		НоваяСтрока.ПолноеНаименование = ФайлДБФ.FULL_NAME;
		НоваяСтрока.УНП = ФайлДБФ.UNP;
		ФайлДБФ.Следующая();
	КонецЦикла;

КонецПроцедуры // ()
```

